// CONTINUE
var count = 10;
for (var i = 0; i < count; i++) {
    if (i % 2 === 0)
        continue;
    //console.log(i);
}
var number = -1;
while (number < 9) {
    number += 1;
    if (number % 2 === 0)
        continue;
    console.log(number);
}
