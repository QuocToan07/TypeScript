//ENUM

// Gom nhóm các constants lại với nhau
enum API_STATUS {
  PENDDING,
  FULFILLED,
  REJECTED, // enum number
}

enum API_STATUS2 {
  PENDDING = "PENDDING",
  FULFILLED = "FULFILLED",
  REJECTED = "REJECTED", // enum string
}
